cols = c(Inventory, Demand),
names_to = "Type",
values_to = "Units"
)
ggplot(region_summary_long, aes(x = Region, y = Units, fill = Type)) +
geom_col(position = "dodge") +
labs(
title = "Inventory vs Demand by Region",
x = "Region",
y = "Units",
fill = "Category"
) +
theme_minimal()
library(tidyverse)
#Inventory data load
data = ("https://raw.githubusercontent.com/japhet125/Data_Science_605_Hws/refs/heads/main/inventory.csv")
inventory_data = read.csv(data)
inventory_data
#Demand data load
data2 = ("https://raw.githubusercontent.com/japhet125/Data_Science_605_Hws/refs/heads/main/demand.csv")
demand_data2 = read.csv(data2)
demand_data2
#If we have a small dataset
region1 <- sum(inventory_data$Region1) - demand_data2$Demand[1]
region1
region2 <- sum(inventory_data$Region2) - demand_data2$Demand[2]
region2
region3 <- sum(inventory_data$Region3) - demand_data2$Demand[3]
region3
#A large dataset we will use this R function for vector
surplus_shortage <- total_inventory - demand_data2$Demand
surplus_shortage
names(surplus_shortage) <- c("Region1", "Region2", "Region3")
surplus_shortage
names(surplus_shortage)[which.min(surplus_shortage)]
load_data <- read.csv("network.csv")
load_data
network_data <- read.csv("network.csv")
network_data
dim(network_data)
dim(network_data)
isSymmetric(network_data)
dim(network_data)
isSymmetric("network_data")
dim(network_data)
diag(network_data)
#Dimensions
dim(network_data)
#Separate user labels from numerical network values
user_labels <- network_data$User
#Dimensions
dim(network_data)
#Separate user labels from numerical network values
user_labels <- network_data$User
adj_matrix <- as.matrix(network_data[, -1])
#Dimensions
dim(network_data)
#Separate user labels from numerical network values
user_labels <- network_data$User
user_labels
adj_matrix <- as.matrix(network_data[, -1])
adj_matrix
rownames(adj_matrix) <- user_labels
dim(adj_matrix)
#Check if the adj matrix is a square
dim(adj_matrix)
nrow(adj_matrix) == ncol(adj_matrix)
#Check if the adj matrix is a square
dim(adj_matrix)
nrow(adj_matrix) == ncol(adj_matrix)
##We get 6x6 and it return True, therefor the adj matrix is a square
#Check the diagonal
diag(adj_matrix)
# Check if the adj matrix is a square
dim(adj_matrix)
nrow(adj_matrix) == ncol(adj_matrix)
## We get 6x6 and it return True, therefor the adj matrix is a square
# Check the diagonal
diag(adj_matrix)
# Check whether all diagonal values are zero
all(diag(adj_matrix) == 0)
# Check whether the matrix is symmetric
isSymmetric(adj_matrix)
# Check if the adj matrix is a square
dim(adj_matrix)
nrow(adj_matrix) == ncol(adj_matrix)
## We get 6x6 and it return True, therefor the adj matrix is a square
# Check the diagonal
diag(adj_matrix)
# Check whether all diagonal values are zero
all(diag(adj_matrix) == 0)
# Check whether the matrix is symmetric
isSymmetric(adj_matrix)
# Check row and cols labels
rownames(adj_matrix)
colnames(adj_matrix)
# Check if the adj matrix is a square
dim(adj_matrix)
nrow(adj_matrix) == ncol(adj_matrix)
## We get 6x6 and it return True, therefor the adj matrix is a square
# Check the diagonal
diag(adj_matrix)
# Check whether all diagonal values are zero
all(diag(adj_matrix) == 0)
# Check whether the matrix is symmetric
isSymmetric(adj_matrix)
# Check row and cols labels
rownames(adj_matrix)
colnames(adj_matrix)
# Check that row and col s name match
identical(rownames(adj_matrix), colnames(adj_matrix))
library(tidyverse)
network_data <- read.csv("network.csv")
network_data
network_data |> autoplot()
network_data |>
ggplot(aes(x = User)) + geom_col() + labs(title = "Network Analysis")
network_data |>
ggplot(aes(network_data)) + geom_col() + labs(title = "Network Analysis")
network_data |>
ggplot(aes(network_data$User)) + geom_col() + labs(title = "Network Analysis")
ggplot(aes(x = network_data)) + geom_bar()
ggplot(network_data  + geom_bar() + labs(title = "Social Media Network Analysis")
ggplot(network_data aes(x = network_data$User) + geom_bar() + labs(title = "Social Media Network Analysis")
ggplot(network_data aes(x = user_labels, y = adj_matrix) + geom_bar() + labs(title = "Social Media Network Analysis")
ggplot(network_data aes(x = user_labels ) + geom_bar() + labs(title = "Social Media Network Analysis")
ggplot(network_data, aes(x = user_labels ) + geom_bar() + labs(title = "Social Media Network Analysis")
ggplot(network_data, aes(x = user_labels )) + geom_bar() + labs(title = "Social Media Network Analysis")
install.packages("igraph")
#install.packages("igraph")
library(igraph)
ggplot(network_data, aes(x = user_labels )) + geom_bar() + labs(title = "Social Media Network Analysis")
#install.packages("igraph")
library(igraph)
network_graph <- graph_from_adjacency_matrix(
adj_matrix,
mode = "undirected",
diag = FALSE
)
#install.packages("igraph")
library(igraph)
network_graph <- graph_from_adjacency_matrix(
adj_matrix,
mode = "undirected",
diag = FALSE
)
network_graph
#install.packages("igraph")
library(igraph)
network_graph <- graph_from_adjacency_matrix(
adj_matrix,
mode = "undirected",
diag = FALSE
)
network_graph
plot(
network_graph,
vertex.label = V(network_graph)$name,
main = "Social Media Network Analysis"
)
eigen(network_data)
eigen(adj_matrix)
eigen_result <- eigen(adj_matrix)
eigen_result$values
eigen_result$vectors
# the dominant eigen value
dominant_index <- which.max(abs(eigen_result$values))
dominant_index
# Check for dominant eigen value
#all.equal(
#  as.vector(),
#  as.vector(dominant_eigenvalue * v)
#)
eigen_result <- eigen(adj_matrix)
eigen_result$values
eigen_result$vectors
# the dominant eigen value
dominant_index <- which.max(abs(eigen_result$values))
dominant_index
dominant_eigenvalue <- eigen_result$values[dominant_index]
dominant_eigenvalue
# Check for dominant eigen value
#all.equal(
#  as.vector(),
#  as.vector(dominant_eigenvalue * v)
#)
eigen_result <- eigen(adj_matrix)
eigen_result$values
eigen_result$vectors
# the dominant eigen value
dominant_index <- which.max(abs(eigen_result$values))
dominant_index
dominant_eigenvalue <- eigen_result$values[dominant_index]
dominant_eigenvalue
v <- eigen_result$vectors[, dominant_index]
v
# Check for dominant eigen value
#all.equal(
#  as.vector(),
#  as.vector(dominant_eigenvalue * v)
#)
eigen_result <- eigen(adj_matrix)
eigen_result$values
eigen_result$vectors
# the dominant eigen value
dominant_index <- which.max(abs(eigen_result$values))
dominant_index
dominant_eigenvalue <- eigen_result$values[dominant_index]
dominant_eigenvalue
v <- eigen_result$vectors[, dominant_index]
v
# Check for dominant eigen value
all.equal(
as.vector(adj_matrix %*% v),
as.vector(dominant_eigenvalue * v)
)
eigen_result <- eigen(adj_matrix)
eigen_result$values
eigen_result$vectors
# the dominant eigen value
dominant_index <- which.max(abs(eigen_result$values))
dominant_index
dominant_eigenvalue <- eigen_result$values[dominant_index]
dominant_eigenvalue
v <- eigen_result$vectors[, dominant_index]
v
# Check for dominant eigen value
all.equal(
as.vector(adj_matrix %*% v),
as.vector(dominant_eigenvalue * v)
)
# The most influential user
abs(v)
which.max(abs(v))
eigen_result <- eigen(adj_matrix)
eigen_result$values
eigen_result$vectors
# the dominant eigen value
dominant_index <- which.max(abs(eigen_result$values))
dominant_index
dominant_eigenvalue <- eigen_result$values[dominant_index]
dominant_eigenvalue
v <- eigen_result$vectors[, dominant_index]
v
# Check for dominant eigen value
all.equal(
as.vector(adj_matrix %*% v),
as.vector(dominant_eigenvalue * v)
)
# The most influential user
abs(v)
user_labels[which.max(abs(v))]
eigen_result <- eigen(adj_matrix)
eigen_result$values
eigen_result$vectors
# the dominant eigen value
dominant_index <- which.max(abs(eigen_result$values))
dominant_index
dominant_eigenvalue <- eigen_result$values[dominant_index]
dominant_eigenvalue
v <- eigen_result$vectors[, dominant_index]
v
# Check for dominant eigen value
all.equal(
as.vector(adj_matrix %*% v),
as.vector(dominant_eigenvalue * v)
)
# The most influential user
abs(v)
user_labels[which.max(abs(v))]
# direct connections
direct_connections <- rowSums(adj_matrix)
data.frame(
User = user_labels,
Direct_Connections = direct_connections,
Eigenvector_Score = abs(v)
)
eigen_result <- eigen(adj_matrix)
eigen_result$values
eigen_result$vectors
# the dominant eigen value
dominant_index <- which.max(abs(eigen_result$values))
dominant_index
dominant_eigenvalue <- eigen_result$values[dominant_index]
dominant_eigenvalue
v <- eigen_result$vectors[, dominant_index]
v
# Check for dominant eigen value
all.equal(
as.vector(adj_matrix %*% v),
as.vector(dominant_eigenvalue * v)
)
# The most influential user
abs(v)
user_labels[which.max(abs(v))]
# direct connections
direct_connections <- rowSums(adj_matrix)
data.frame(
User = user_labels,
Direct_Connections = direct_connections,
Eigenvector_Score = abs(v)
)
library(tidyverse)
network_data <- read.csv("network.csv")
network_data
#Dimensions
dim(network_data)
#Separate user labels from numerical network values
user_labels <- network_data$User
user_labels
adj_matrix <- as.matrix(network_data[, -1])
adj_matrix
rownames(adj_matrix) <- user_labels
# Check if the adj matrix is a square
dim(adj_matrix)
nrow(adj_matrix) == ncol(adj_matrix)
## We get 6x6 and it return True, therefor the adj matrix is a square
# Check the diagonal
diag(adj_matrix)
# Check whether all diagonal values are zero
all(diag(adj_matrix) == 0)
# Check whether the matrix is symmetric
isSymmetric(adj_matrix)
# Check row and cols labels
rownames(adj_matrix)
colnames(adj_matrix)
# Check that row and col s name match
identical(rownames(adj_matrix), colnames(adj_matrix))
#install.packages("igraph")
library(igraph)
network_graph <- graph_from_adjacency_matrix(
adj_matrix,
mode = "undirected",
diag = FALSE
)
network_graph
plot(
network_graph,
vertex.label = V(network_graph)$name,
main = "Social Media Network Analysis"
)
eigen_result <- eigen(adj_matrix)
eigen_result$values
eigen_result$vectors
# the dominant eigen value
dominant_index <- which.max(abs(eigen_result$values))
dominant_index
dominant_eigenvalue <- eigen_result$values[dominant_index]
dominant_eigenvalue
v <- eigen_result$vectors[, dominant_index]
v
# Check for dominant eigen value
all.equal(
as.vector(adj_matrix %*% v),
as.vector(dominant_eigenvalue * v)
)
# The most influential user
abs(v)
user_labels[which.max(abs(v))]
# direct connections
direct_connections <- rowSums(adj_matrix)
data.frame(
User = user_labels,
Direct_Connections = direct_connections,
Eigenvector_Score = abs(v)
)
# direct connections
direct_connections <- rowSums(adj_matrix)
results <- data.frame(
User = user_labels,
Direct_Connections = direct_connections,
Eigenvector_Score = abs(v)
)
results[order(-results$Eigenvector_score), ]
library(tidyverse)
network_data <- read.csv("network.csv")
network_data
#Dimensions
dim(network_data)
#Separate user labels from numerical network values
user_labels <- network_data$User
user_labels
adj_matrix <- as.matrix(network_data[, -1])
adj_matrix
rownames(adj_matrix) <- user_labels
# Check if the adj matrix is a square
dim(adj_matrix)
nrow(adj_matrix) == ncol(adj_matrix)
## We get 6x6 and it return True, therefor the adj matrix is a square
# Check the diagonal
diag(adj_matrix)
# Check whether all diagonal values are zero
all(diag(adj_matrix) == 0)
# Check whether the matrix is symmetric
isSymmetric(adj_matrix)
# Check row and cols labels
rownames(adj_matrix)
colnames(adj_matrix)
# Check that row and col s name match
identical(rownames(adj_matrix), colnames(adj_matrix))
#install.packages("igraph")
library(igraph)
network_graph <- graph_from_adjacency_matrix(
adj_matrix,
mode = "undirected",
diag = FALSE
)
network_graph
plot(
network_graph,
vertex.label = V(network_graph)$name,
main = "Social Media Network Analysis"
)
eigen_result <- eigen(adj_matrix)
eigen_result$values
eigen_result$vectors
# the dominant eigen value
dominant_index <- which.max(abs(eigen_result$values))
dominant_index
dominant_eigenvalue <- eigen_result$values[dominant_index]
dominant_eigenvalue
v <- eigen_result$vectors[, dominant_index]
v
# Check for dominant eigen value
all.equal(
as.vector(adj_matrix %*% v),
as.vector(dominant_eigenvalue * v)
)
# The most influential user
abs(v)
user_labels[which.max(abs(v))]
# direct connections
direct_connections <- rowSums(adj_matrix)
esults <- data.frame(
User = user_labels,
Direct_Connections = direct_connections,
Eigenvector_Score = abs(v)
)
results[order(-results$Eigenvector_Score), ]
library(tidyverse)
network_data <- read.csv("https://raw.githubusercontent.com/japhet125/Data_Science_605_Hws/refs/heads/main/network.csv")
network_data
#Dimensions
dim(network_data)
#Separate user labels from numerical network values
user_labels <- network_data$User
user_labels
adj_matrix <- as.matrix(network_data[, -1])
adj_matrix
rownames(adj_matrix) <- user_labels
# Check if the adj matrix is a square
dim(adj_matrix)
nrow(adj_matrix) == ncol(adj_matrix)
## We get 6x6 and it return True, therefor the adj matrix is a square
# Check the diagonal
diag(adj_matrix)
# Check whether all diagonal values are zero
all(diag(adj_matrix) == 0)
# Check whether the matrix is symmetric
isSymmetric(adj_matrix)
# Check row and cols labels
rownames(adj_matrix)
colnames(adj_matrix)
# Check that row and col s name match
identical(rownames(adj_matrix), colnames(adj_matrix))
#install.packages("igraph")
library(igraph)
network_graph <- graph_from_adjacency_matrix(
adj_matrix,
mode = "undirected",
diag = FALSE
)
network_graph
plot(
network_graph,
vertex.label = V(network_graph)$name,
main = "Social Media Network Analysis"
)
eigen_result <- eigen(adj_matrix)
eigen_result$values
eigen_result$vectors
# the dominant eigen value
dominant_index <- which.max(abs(eigen_result$values))
dominant_index
dominant_eigenvalue <- eigen_result$values[dominant_index]
dominant_eigenvalue
v <- eigen_result$vectors[, dominant_index]
v
# Check for dominant eigen value
all.equal(
as.vector(adj_matrix %*% v),
as.vector(dominant_eigenvalue * v)
)
# The most influential user
abs(v)
user_labels[which.max(abs(v))]
# direct connections
direct_connections <- rowSums(adj_matrix)
esults <- data.frame(
User = user_labels,
Direct_Connections = direct_connections,
Eigenvector_Score = abs(v)
)
results[order(-results$Eigenvector_Score), ]
# direct connections
direct_connections <- rowSums(adj_matrix)
results <- data.frame(
User = user_labels,
Direct_Connections = direct_connections,
Eigenvector_Score = abs(v)
)
results[order(-results$Eigenvector_Score), ]
